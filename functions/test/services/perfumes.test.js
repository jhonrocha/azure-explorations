const tap = require('tap')
const { getSephora } = require('../../services/perfumes.js')

tap.test('Test Sephora Return', async () => {
  // const sep = await getSephora()
  // console.log(sep)
  tap.pass('green')
})
